# 🧠 MNIST Digit Clustering with t-SNE & K-Means

An interactive Streamlit dashboard that visualizes handwritten digit clusters using t-SNE, PCA, and K-Means.

## 📌 Project Overview

This project explores unsupervised learning and visualization techniques on the MNIST digit dataset to understand:
* How digits group in high-dimensional space
* Where clusters overlap
* Which samples are misclassified and why

It is especially useful for AI model debugging, representation analysis, and exploratory data analysis (EDA).

## 🧩 How It Works

### 1️⃣ Data Loading
* Uses `sklearn.datasets.load_digits`
* Images are flattened and normalized to `[0,1]`
* Labels are used only for evaluation, not training

### 2️⃣ PCA (Preprocessing)
* PCA reduces dimensionality while preserving 95% variance
* Benefits:
  * Removes noise
  * Speeds up t-SNE
  * Improves cluster stability

### 3️⃣ t-SNE (Visualization)
* Projects high-dimensional data into 2D space
* Preserves local neighborhood structure
* Controlled via perplexity slider in the UI

### 4️⃣ K-Means Clustering
* Applied on t-SNE embeddings
* Number of clusters (`k`) is user-controlled
* Each cluster is assigned a dominant digit label

### 5️⃣ Evaluation & Insights
* Silhouette Score measures cluster separation quality
* Misclassified samples are detected by comparing:
  * Dominant cluster label vs true label
* Misclassified digits are displayed visually

### 6️⃣ Interactive Dashboard (Streamlit)
* Sliders for:
  * Number of clusters
  * t-SNE perplexity
* Interactive Plotly scatter plots
* Side-by-side PCA vs t-SNE comparison

## 🖥️ Dashboard Features

* 📊 Silhouette Score metric
* 🔍 Cluster size & dominant digit analysis
* ❌ Misclassified digit viewer
* 🆚 PCA vs t-SNE visual comparison
* 🎛️ Real-time parameter tuning

## 📷 Results & Visualizations

The following images are generated by the application and stored in the `results/` folder.

### 2D t-SNE + K-Means Clustering
![t-SNE Clustering](Result/Screenshot%202026-01-31%20115257.png)

### PCA Visualization
![PCA Visualization](Result/Screenshot%202026-01-31%20131809.png)

### Misclassified Digits
![Misclassified Samples](Result/Screenshot%202026-01-31%20131720.png)

### PCA & t-SNE Comparision 
![PCA & t-SNE Comparision](Result/Screenshot%202026-01-31%20115332.png)

## 🛠️ Tech Stack

* Python
* Streamlit – Interactive dashboard
* scikit-learn – PCA, t-SNE, K-Means
* Plotly – Interactive visualizations
* NumPy / Pandas
* Matplotlib

## 📦 Requirements

```
streamlit==1.32.2
scikit-learn==1.4.0
numpy==1.26.4
plotly==5.18.0
matplotlib==3.8.3
pandas==2.2.1
```

## ▶️ How to Run

### Local

```bash
pip install -r requirements.txt
streamlit run app.py
```

Then open 👉 `http://localhost:8501`

### Docker (Optional)

```bash
docker build -t mnist-clustering .
docker run -p 8501:8501 mnist-clustering
```

## 🧠 Why This Project Is Useful

* Helps debug feature representations in ML models
* Identifies confusing digit pairs (e.g., 3 vs 5, 8 vs 9)
* Demonstrates unsupervised learning evaluation
* Useful in:
  * OCR systems
  * Handwriting recognition
  * Representation learning analysis
  * Model drift detection – Visualizes changes in data distribution over time.
  * Data quality auditing – Identifies noisy, mislabeled, or outlier samples.
  * Active learning support – Highlights ambiguous samples for human review.

## 🧪 Future Enhancements

* MNIST-C (corrupted dataset) analysis
* UMAP visualization
* Cluster purity & entropy metrics
* Model embedding comparison (CNN vs raw pixels)


### 👤 Author
**Sai Kiran Tungana**  
GitHub: https://github.com/Saikiranabhi

---
